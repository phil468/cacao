import {Component,inject} from '@angular/core';import {CurrencyPipe} from '@angular/common';import {RouterLink} from '@angular/router';import {IonContent,IonHeader,IonToolbar,IonTitle,IonItem,IonLabel,IonButton} from '@ionic/angular/standalone';import {CartService} from '../../core/cart.service';import {MobileNavigationComponent} from '../../shared/mobile-navigation.component';@Component({standalone:true,imports:[CurrencyPipe,RouterLink,IonContent,IonHeader,IonToolbar,IonTitle,IonItem,IonLabel,IonButton,MobileNavigationComponent],template:`<ion-header><ion-toolbar><ion-title>Tu carrito</ion-title></ion-toolbar></ion-header><ion-content>@for(item of cart.items();track item.variantId){<ion-item><ion-label>{{item.name}} × {{item.quantity}}<p>{{item.priceAmount/100|currency:'PEN':'symbol':'1.2-2'}}</p></ion-label><ion-button fill="clear" (click)="cart.remove(item.variantId)">Quitar</ion-button></ion-item>}<h2>Total: {{cart.total()/100|currency:'PEN':'symbol':'1.2-2'}}</h2><ion-button expand="block" routerLink="/checkout">Continuar al checkout</ion-button></ion-content><app-mobile-navigation />`})export class CartPage{cart=inject(CartService);}
